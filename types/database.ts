/**
 * Placeholder — replace with generated types once local Supabase is running:
 *
 *   supabase gen types typescript --local > types/database.ts
 *
 * Until then this keeps the codebase type-safe with a loose `unknown` fallback.
 */
export type Database = Record<string, unknown>
