# /lib/utils

Framework-agnostic helper functions.

## Planned utilities
- `cn.ts` — `clsx` + `tailwind-merge` className helper
- `format.ts` — date, number, and word-count formatters (EN + VI locale-aware)
- `slugify.ts` — generate URL-safe slugs
